# Debugging

When investigating a bug, a failing test, or unexpected behavior, load the `ce:systematic-debugging` skill and find the root cause before changing code. For slow code, load `ce:optimizing-performance` and measure first.

The two-attempt budget in the Debugging section of `AGENTS.md` applies: two failed fixes with no new diagnostic in between means stop, summarize what you learned, and ask.
