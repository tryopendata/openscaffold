# {{project_name}}

Instructions for coding agents working in this repository. Keep this file under about 200 lines: it is loaded at the start of every session. Put detail that matters only for one area in a doc and link it from Deeper context.

<!-- openscaffold:fill Every "openscaffold:fill" comment marks something to write from the real project. Replace each one with real content (or delete the section if it truly doesn't apply) and remove the comment. `openscaffold verify` fails while any remain. -->

## Overview

<!-- openscaffold:fill Two or three sentences: what this project is, who uses it, and the main technologies. -->

## Commands

Run these from the repo root. They are the only supported way to build, test, and run the project; if a command here is wrong, fix it here in the same change.

| Command | What it does |
| --- | --- |
<!-- openscaffold:fill One row per real command (Makefile target or package script): install, dev, test, a single test, lint, format, typecheck, build, and any service start/stop. Copy them from the Makefile or scripts and run each once to confirm it works. Include how to run one test file. -->

## Architecture map

<!-- openscaffold:fill A short tree of the top-level directories and what lives in each, plus one line on how requests or data flow between the parts. Only directories that exist. -->

## Conventions

<!-- openscaffold:fill The patterns this codebase follows that an agent would not guess: module boundaries, where config is loaded, naming, how generated code is produced and that it is never hand-edited. -->

- Prefer the project's existing patterns and dependencies over new ones. Add a dependency only when nothing in the project already covers the need.
- Keep changes scoped to the task. Mention unrelated problems you notice instead of fixing them in the same change.

## Testing

<!-- openscaffold:fill Test layers (unit, integration, end-to-end), which runner each uses, what is real and what is faked, the test harness files to build on, and anything tests need running first (a database, a .env). -->

- Behavior-focused tests against real dependencies where practical. Fake only what crosses the network to third parties.
- A bug fix starts with a test that reproduces the bug.
- Weakening or skipping a test to get green is a failure, not a fix.

## Error handling

<!-- openscaffold:fill The project's error pattern: error types, where errors are caught, logged, and turned into responses or exit codes. -->

- Never swallow an error. Handle it, or add context and pass it on.
- Log an error once, at the boundary that handles it.

## Debugging

- If a fix hasn't worked after two attempts with no new diagnostic step in between, stop. Write down what you learned, list two or three other root-cause hypotheses, and ask which to pursue. Running a new diagnostic between attempts resets the count.
- Read logs and error output before guessing.

<!-- openscaffold:fill Where the running app writes logs, how to get verbose output, and any known failure signatures. Delete this comment if there is nothing project-specific yet. -->

## Build workarounds

Things in the tree that exist to get a build through, not because they are right. Each one says when it can be removed. Re-check them when upgrading the tools involved.

| Where | What and why | Remove when |
| --- | --- | --- |
<!-- openscaffold:fill Add a row for every workaround, patch, pin, or disabled check you had to introduce while setting the project up. If there are none, write "None yet." under the table header and remove the empty table. -->

## Deferred

Explicitly out of scope for now, so nobody mistakes them for forgotten work.

<!-- openscaffold:fill List what was deliberately left out (e.g. CI deploys, a production database, a second platform), or write "Nothing yet." -->

## Pending user actions

Steps only a human can do (accounts, API keys, DNS, signing identities). Check here before reporting one of these as a problem.

<!-- openscaffold:fill List each with how to confirm it is done, or write "None." -->

## Deeper context

Read these only when the task needs them.

| Need | Read |
| --- | --- |
<!-- openscaffold:fill One row per doc that exists (README, architecture notes, runbooks). Remove the table if there are none yet. -->
