# Copy to .env and adjust. .env is gitignored; never commit real secrets.

# Dev server port. `openscaffold verify` and the Makefile read this.
PORT_WEB=4321

# Only PUBLIC_-prefixed vars reach client code, baked in at build time.
