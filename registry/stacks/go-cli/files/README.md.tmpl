# {{project_name}}

<!-- One or two sentences on what this tool does and who it's for. -->

## Install

```bash
go install <module path>/cmd/{{project_slug}}@latest
```

Or build from source:

```bash
make build
./bin/{{project_slug}} --help
```

## Usage

```bash
{{project_slug}} --help
{{project_slug}} --version
```

<!-- Add the main workflows here: the two or three commands people run most. -->

Every command accepts `--json` for machine-readable output. See [docs/configuration.md](docs/configuration.md) for config files, environment variables, and flags.

## Development

Requires Go, make, and [golangci-lint](https://golangci-lint.run/).

| Command | What it does |
|---|---|
| `make build` | Build `bin/{{project_slug}}` |
| `make test` | Unit tests |
| `make test-integration` | Integration tests against the built binary |
| `make lint` | golangci-lint |
| `make coverage` | Race-enabled tests with a coverage floor |
| `make ci` | Everything CI runs |

See [docs/DEVELOPMENT.md](docs/DEVELOPMENT.md) for more.

## License

Copyright (c) {{year}} {{author}}
